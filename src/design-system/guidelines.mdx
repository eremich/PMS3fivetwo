import { Markdown, Meta } from "@storybook/addon-docs/blocks";
import rules from "../../docs/design-system.md?raw";

<Meta title="Guidelines/Design system" />

{/* The rules live in docs/design-system.md and are rendered here, so there is only one copy to keep current. */}

<Markdown>{rules}</Markdown>
